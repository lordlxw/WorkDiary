业务需求：
1、中介引用逻辑：识别中介引用的我方消息，如果引用内容为我方指令，则识别指令中的询价单ID（例如T0001），此条消息中的操作局限于此单。如果可以识别中介意图，则进入意图处理，例如成交，撤单，改价。如果无法判断意图或者处理意图出错，则自动提交审核。中介引用其自身消息则直接提交审核

20小时

2、实盘客户端：弹出消息后，需将当前光标切回当前下单窗口

2小时

3、保存界面配置的时候 加上是否远期的保存

2小时


吴老师：
1、界面发出后，焦点回到发出指令的下单界面	4
2、实盘服务端——中介引用逻辑：识别中介引用的是我方消息还是中介自己的消息，如果引用内容为我方指令，则识别指令中的询价单ID（例如T0001），此条消息中的操作局限于此单。
3、中介引用其自身消息则直接提交审核。	20
4、实盘端——由GPU改为软件渲染时，聊天框闪烁，CPU升高的问题	12
5、实盘端——UI改变，省去左侧导航页面，样式待和钟老师沟通	12
6、实盘端——UI引用消息样式改变	8
联调测试	16
合计：	72

新业务：
1、保存界面配置的时候 加上是否远期的保存 ✔
2、未平界面 时间只要时分秒，价格变色 ✔
3、审核复议界面UI 
4、倒计时UI改变，放在当前窗口并且置顶。✔
5、隐藏违约单界面 操作按键
6、UAT环境配合调试，数据库同步，问题发现及修复（自动撤单和在吗都无）5小时
7、配合研究员调试，数据库配置权限

Bug：
1、模拟端/实盘端 去除勾了之后还能登录
2、改价超过5位数显示不全
3、强平提示的问题，为什么没刷新

待讨论：
1、延时的问题如何解决。

研究员提问：
1、右侧留白。

补充工时（下两周）
1、可以默认+1吗，因为+0已经不可做了，只有+1了。然后从18切到19，默认放+0，因为+1不可选
2、平仓框 挡住，可拖动。
 
11/12
1、bug修复，多开下单界面，当一个占用中介时，别的界面中介未改变。 ✔
2、bug修复，当放大盘口的时候，会刷回到基础样式。 ✔
3、bug修复，平仓>1000万的时候还能平1000万  感觉要做吗？


11/13
1、有个bug，我下单6000万未接收，我改价，改到2000万 询价显示的是2000万，但消息没发出去。
2、保存界面配置的时候 加上是否远期的保存 ✔
3、中介引用识别，方向判别（0是引用研究员消息，1是中介引用自己的消息）✔


11/14
1、有个bug，tick图曲线max 会超过。 ✔
2、窗口无法置顶，好像是owner的关系？？
3、未平界面 时间只要时分秒，价格变色 ✔

11/18
4、倒计时UI改变，放在当前窗口并且置顶。✔

11/22
   <dxc:AxisX2D.WholeRange>
       <dxc:Range AutoSideMargins="False" SideMarginsValue="1"/>
   </dxc:AxisX2D.WholeRange>
这个chartcontrol可以控制，AutoSideMargins 为 True，会自动为轴的两侧添加一定的间距