7.11
宝钢用户行为观察软件

任务：
1、把sqlite本地数据推送一份至Mysql
2、用户一远程上去就启动该程序，当然需要隐藏
3、加表字段，用户表，做关联

公司工作站搭建mysql环境
密码123456
下午把Mysql环境和数据表建立起来了


7.12
感觉原来的程序负荷太高，我给代码做了重构，只需要它的服务类。其他的交给小孔那做。

7.13
卧槽，EFCore我整了半天

Code ---》数据库
需要先一个类继承自DbContext然后定义好表结构

自动生成步骤如下：
1、需要添加Nuget包：Microsoft.EntityFrameworkCore，Microsoft.EntityFrameworkCore.Tools、Microsoft.EntityFrameworkCore.Relational
2、有一个类继承自DbContext且有定义好的DbSet
3、将路径移到该项目下，代码：
dotnet ef migrations add InitialMigrationSecond 命令只是创建了一个迁移文件，它描述了要应用到数据库的模型变更。执行迁移命令并不会直接创建数据库。
4、上面只是迁移，创建表结构命令：
dotnet ef database update

还发现一个问题，必须要以管理员身份运行

7.14
今天查了一下问题，终于有些苗头了，问题在于我的读写者互锁住了

两天了，才查出这一个bug，问题就在于读锁没有解除

7/18
孟总开了个会，总结最近发生的问题以及个人和公司未来发展的方向
公司很重要的一环就是现金流
要有危机意识，加油努力！

7.19
SystemEvent事件是在用户层面的
当开启了RDP之后，其实Session切换事件的触发跑到了系统层面，它也自然捕捉不到了

另一个bug当一个userid未被访问过时，程序会卡死，应该跟没有加lock有关。


7.21
目前的任务有：
1、新建git分支，master和测试要区分开
2、新增软件许可证，在注册表层面加
3、打包程序为一个exe程序，方便安装部署

7.24
本周任务有：
1、把监控用户行为的程序安排好，如何部署，新增许可证时间
2、磨辊系统又要做了，把老程序看看
3、学习一下Docker

我自己感觉要新加一张log表

netstat -i tu0 1 ——Linux下查看实时网络流量


自己加一个对注册表的加密规则

7.31
守护进程的概念，如果安装了mysql，除了服务，会在程序一栏里看到mysqld这个程序，这个其实就是守护进程，进程名后面加d的。
进程和会话：
进程是为了内部的执行的线程提供一个空间和环境，而会话则是为内部所有的进程提供一个执行的空间和环境。